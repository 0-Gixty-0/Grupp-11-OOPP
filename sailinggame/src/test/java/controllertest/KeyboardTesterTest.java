package controllertest;

import org.junit.Test;

public class KeyboardTesterTest {
    // Class is already a test class itself so no tests are needed.

    @Test
    public void mainTest() {
        // This method is only used to run the program, so no tests are needed.
    }
}